# multisim-tbc-_Semiconductors-and-active-elements

Designed circuits in answers (descriptions in Polish).

Semiconductors and active elements
a. Rectifiers

1. Run the project of a half-wave rectifier.
Observe the changes in the ripples of the rectified signal depending on the value of the filter capacitor C (similarly to task 1).
(designed circuit in answers)
Fig.1
b. Power supplies

1. Run the project of a stabilized power supply stabilizer integrated circuit.

Measure the changes in the output voltage resulting from changes in the power supply load and from changes in the input rectified voltage. Use the power supply 
diagram as below.
(designed circuit in answers)
Fig.2

c. Transistor amplifiers in OE configuration

The basic parameters of each amplifier are: gain, input and output impedance, and the bandwidth of transmitted frequencies.
To measure the appropriate impedance, create a voltage divider by 2. Then the input impedance will be equal to the series resistance of the divider (Rinput), 
and the output impedance will be equal to the load resistance of the divider (Routput). This is explained in the figure below.
(designed circuit in answers)
Fig.3

Make the appropriate measurements, i.e. measure: gain, input and output impedance and the bandwidth of the transmitted frequencies for the transistor amplifier 
as below. Use an oscilloscope and the method described above to measure the gain and input/output impedance. Use a frequency counter (Bode plotter) to determine 
the bandwidth of the transmitted frequencies.

Amplifier in OE configuration
(designed circuit in answers)
Fig.4

d. Differential amplifiers

Implement the project as in the figure below and determine:
- differential gain and
- common.
(designed circuit in answers)
Fig.5

Check how the same amplifier works, but in which a current source was used. Determine the same parameters as you implemented for the previous amplifier/
(designed circuit in answers)
Fig.6
e. Prepare a report on the completion of all points of this exercise

Each report should be provided with the following heading and include:
a detailed description of all completed points of the exercise, along with your own conclusions regarding the work performed.
